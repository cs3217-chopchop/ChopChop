struct CompleteRecipeSessionStage {

    // get ahold of recipe's ingredients, check against ingredient store
    // note of ingredients with different units of measurement
    // help to update ingredient store
    // put a max (whats currently in the store) and min (0) to how much u can delete from the store

    // check if user want to save recipe to recipe in database
}
